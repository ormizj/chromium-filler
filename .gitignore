.idea
node_modules
dist
test-results
playwright-report
*.log
.DS_Store
*.zip
.pkgtmp
.claude/settings.local.json
.playwright-mcp
# Screenshots taken while checking a surface. Root-level only — design/ and any
# fixture image are still tracked. `undefined/` is where they land when the
# output path is left unset.
/*.png
undefined/
